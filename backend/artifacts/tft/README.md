# Bundled TFT checkpoint

`best.ckpt` is a small PyTorch Forecasting Temporal Fusion Transformer demonstration checkpoint trained on the bundled UCI MT_001 sample. It proves the complete TFT training and inference path.

For an actual building deployment, retrain the checkpoint on that building's own 15-minute history. Do not treat the demonstration checkpoint as a universally calibrated production model.
