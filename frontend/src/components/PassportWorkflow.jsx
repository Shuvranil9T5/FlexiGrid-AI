import PassportCard from "./PassportCard";
export default function PassportWorkflow({passports,onEdit,onStatus}) {return <section className="card"><div className="titleline"><div><h2>Flexibility Passport workflow</h2><p>Review every candidate before it can enter optimization.</p></div><span className="tag">Confirm · Edit · Reject</span></div>{passports.map((passport,index)=><PassportCard key={passport.pattern_id} passport={passport} index={index} onEdit={onEdit} onStatus={onStatus}/>)}{passports.length===0&&<p>No recurring candidate load was detected. More days of data may be required.</p>}</section>}
