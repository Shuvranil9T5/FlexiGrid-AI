export default function ScheduleTimeline({schedule=[]}) {return <section className="card"><h2>24-hour schedule timeline</h2><div className="timeline-axis">{[0,6,12,18,24].map(h=><span key={h}>{String(h).padStart(2,"0")}:00</span>)}</div>{schedule.map(item=><div className="timeline-row" key={item.pattern_id}><b>{item.label}</b><div className="track"><span className="bar original" style={{left:`${item.original_start_slot/96*100}%`,width:`${item.duration_slots/96*100}%`}}>Original</span><span className="bar optimized" style={{left:`${item.recommended_start_slot/96*100}%`,width:`${item.duration_slots/96*100}%`}}>New</span></div></div>)}{schedule.length===0&&<p>No feasible shift was generated.</p>}</section>}
