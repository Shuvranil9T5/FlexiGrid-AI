export default function FaultAlerts({alerts=[]}) {return <section className="card"><div className="titleline"><div><h2>Energy waste and fault screening</h2><p>Statistical alerts require operator verification and are not equipment diagnoses.</p></div><span className="tag">{alerts.filter(a=>a.severity!=="info").length} alerts</span></div><div className="alert-grid">{alerts.map(alert=><article className={`fault-alert ${alert.severity}`} key={alert.code}><span>{alert.severity}</span><div><h3>{alert.title}</h3><p>{alert.description}</p></div></article>)}</div></section>}
